// WeatherGPT — System-wide Constants & Centralized Defaults
// Single source of truth for fallback district, state, and coordinates.

export const DEFAULT_DISTRICT = "Raigad";
export const DEFAULT_STATE = "Maharashtra";
export const DEFAULT_LANGUAGE = "en-IN" as const;
export const DEFAULT_COORDINATES = {
  latitude: 18.5158,
  longitude: 73.1822,
};
